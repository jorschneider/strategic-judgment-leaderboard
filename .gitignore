.DS_Store
node_modules/

# Raw model responses from the harness (large, regenerated by run.mjs).
harness/results.json
# scores.json IS tracked — it is the artifact the site loads. The committed
# copy is the dry-run sample; replace it with a real run to publish numbers.
